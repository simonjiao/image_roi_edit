# Local Flow Hardening Plan and Checklist

本文档沉淀本地 ROI 文字替换流程的强化方案。目标不是继续增加经验参数，而是把流程升级为：

```text
原图参考画像
-> 阶段化本地求解
-> 本地指标主导候选选择
-> 视觉模型阶段验收
-> 可解释迭代和失败交付
```

本方案不接受折中交付。若候选没有通过硬校验、阶段门禁和最终视觉验收，只能作为 rejected candidate 展示，不能标记为成功应用。

文字形态门禁、放置策略、现有差距和分层联合优化设计见
[`docs/text_shape_joint_optimization_design.md`](text_shape_joint_optimization_design.md)。当本 checklist 与该设计文档描述同一能力时，checklist 记录实施状态，设计文档记录目标结构和实施顺序。
本文件同时作为“完善本地流程”的唯一实施 checklist，不再另建独立覆盖清单。

## Checklist 使用规则

本文件只有“设计目标转换情况”一节是权威实施 checklist。旧的阶段切片、阶段说明和实现记录不再用
`[x]` 表示完成，避免把“已有部分代码”误写成“目标能力已验收”。所有完成项必须满足：

1. 可实施：能落到具体代码、prompt、报告、fixture、测试或 UI/CLI 输出。
2. 可验证：条目中必须说明关闭它的证据来源。
3. 不宽泛：如果设计目标包含多个子要求，不能用一个大 `[x]` 覆盖。
4. 不补丁化：如果某项只能靠一次性参数或单图规则通过，先补 detector、stage 定义或测试，再标记完成。

## 设计目标转换情况

本节把 `staged_roi_pipeline_design.md` 和
`text_shape_joint_optimization_design.md` 中的目标拆成可实施、可验证的 checklist 项。
`[x]` 只表示该条目标已经完整实现，并且有同条或相邻说明中的测试、报告、fixture 或稳定输出证据；`[ ]` 表示还需要代码、测试、报告或文档同步。
每个 `[ ]` 都必须能用测试、CLI/Web 输出、`progress.jsonl`、`result.json`、stage evidence
或 fixture 回归关闭，不能只靠文字说明关闭。

### A. 三层流程边界

- [x] 五个本地 stage 必须成为代码、prompt、报告和测试的唯一权威结构：`hard_boundary`、`text_shape`、`ink_gray_balance`、`photo_texture`、`background_cleanup`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_authority.py` 覆盖 stage order、runtime prompt 资产、candidate-rank prompt payload、progress/result report schema 都只使用五个公开 stage。
- [x] `StageSpec` 必须有稳定字段契约：`id`、`display_name`、`blocks_next`、`detect`、`optimization_steps`、`allowed_patch_keys`、`blocked_patch_keys`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stage_spec_field_contract_and_reports`。
- [x] `StageResult` 必须有稳定字段契约：`stage_id`、`display_name`、`passed`、`blocks_next`、`severity`、`issues`、`reason`、`allowed_patch_keys`、`blocked_patch_keys`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stage_result_field_contract_and_stage_context`。
- [x] 每个 stage 必须能回答四个问题：是否通过、失败是否阻塞后续、允许哪些参数、禁止哪些参数；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stage_result_field_contract_and_stage_context`。
- [x] 增加前置安全流程验收：`orientation_check`、`field_roi_selection`、`slot_quality_gate`、`protected_text_guard` 必须在候选生成前完成；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_pre_candidate_gates.py` 验证 `src/roi_image_edit/pre_candidate_gates.py` 固定四个前置 gate 顺序并区分 slot quality 与 protected text failure，`tests/test_slot_quality_gate.py::SlotQualityGateTest.test_failed_slot_quality_stops_before_candidate_generation` 和 `test_protected_text_guard_failure_stops_before_candidate_generation` 验证失败样例 `candidate_count=0`、不生成候选且 progress 先记录 `pre_candidate_gate_failed`，`tests/test_failure_artifacts.py::FailureArtifactsTest.test_auto_roi_failure_preserves_rejected_artifacts_and_result_evidence` 验证 auto ROI 找不到旧值时 `progress.jsonl` 和 failure report 写入 `field_roi_selection` 前置失败。
- [x] 增加阶段门禁顺序验收：`src/roi_image_edit/stage_policy.py` 的 `STAGE_ORDER` 必须与本 checklist 的五阶段顺序一致；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stage_order_is_the_five_stage_contract`。
- [x] 增加阶段内 Optimization Step 验收：每个候选报告必须区分 `stage_id` 和 `optimization_step`，不能把 Optimization Step 当成新 stage；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_optimization_step_is_not_a_stage_id`、`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_filter_report_declares_secondary_impacts_and_rejects_cross_stage_primary` 和 `tests/test_run_artifacts.py::RunArtifactsTest.test_result_audit_payload_strips_data_urls_but_keeps_stage_artifact_fields`。
- [x] 增加视觉终检边界验收：视觉模型只能看本地 top candidates；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_vision_candidate_boundary.py::VisionCandidateBoundaryTest.test_region_vision_request_uses_top_candidates_with_stage_context` 和 `tests/test_run_artifacts.py::RunArtifactsTest.test_vision_candidate_request_records_limit_and_stage_context` 验证 `vision_candidate_request.json`、`candidate_count <= vision_candidate_limit`、本地 `stage_context_by_candidate` 完整且非 top 候选不进入视觉 prompt。

### B. 旧 7 类诊断关注点映射到当前 5 个 stage

- [x] `slot_alignment` 必须映射到 `hard_boundary` 的 ROI/slot 安全条件和 `text_shape.slot_alignment_search`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_stage_gate_exposes_diagnostic_concern_mapping_as_stage_evidence` 验证 stage gate 输出 `diagnostic_concern_mapping`，其中 `slot_alignment.current_stages=(hard_boundary,text_shape)`、`optimization_steps` 含 `slot_quality_gate`、`report_fields` 含 `slot_quality_report`。
- [x] `font_structure` 必须映射到 `text_shape.font_style_search`、`font_size_search`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_font_stroke_tone_and_edge_concerns_map_to_named_within_stage_steps` 验证映射，`tests/test_stage_contracts.py::StageContractsTest.test_font_structure_failure_blocks_ink_gray_primary_patches` 验证字体失败时 blocking stage 为 `text_shape` 且 `opacity_delta` 这类 ink patch 被拒绝。
- [x] `pose_geometry` 必须映射到 `text_shape.pose_shear_search`，且不能固化某张图的左倾/右倾；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_pose_geometry_maps_to_text_shape_and_reports_slot_neighbor_shear` 验证 `pose_geometry.current_stages=(text_shape)`、`optimization_steps=(pose_shear_search)`、报告字段包含 `char_pose_metrics`/`local_pose_issues`，且 `local_pose_issues` 从 `source_slot_shear`、`neighbor_shear` 推导 `reference_shear`。
- [x] `stroke_body` 必须映射到 `text_shape.stroke_body_search`，且在真实笔画体量未过时不能被 `edge_quality` 或 `photo_texture` 抢先处理；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_font_stroke_tone_and_edge_concerns_map_to_named_within_stage_steps` 验证映射，`tests/test_stage_contracts.py::StageContractsTest.test_stroke_body_failure_blocks_gray_cleanup_and_photo_noise` 验证笔画体量失败时 blocking stage 为 `text_shape`，`background_cleanup` 和 `photo_texture` 补丁被拒绝。
- [x] `tone_gray` 必须映射到 `ink_gray_balance.core_black_search`、`mid_gray_body_search`、`opacity_search`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_font_stroke_tone_and_edge_concerns_map_to_named_within_stage_steps` 验证映射，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_ink_gray_dispatch_generates_opposite_directions_for_black_core_and_light_core` 验证黑芯过量和核心偏浅生成相反方向候选。
- [x] `edge_quality` 必须拆到 `ink_gray_balance.outer_gray_control` 和 `photo_texture.edge_breakup_match`，并记录拆分依据；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_font_stroke_tone_and_edge_concerns_map_to_named_within_stage_steps` 验证拆分映射，`tests/test_stage_contracts.py::StageContractsTest.test_ink_gray_stage_rejects_photo_noise_as_primary_fix` 验证灰边/黑灰阶段不会用 photo noise 作为主修复。
- [x] `photo_texture` 必须映射到 `photo_texture.blur_match`、`edge_breakup_match`、`noise_texture_match`、`jpeg_texture_match`、`residual_retexture`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_concerns.py::StageConcernMappingTest.test_photo_texture_concern_maps_to_named_within_stage_steps` 验证五个阶段内步骤映射，`tests/test_stage_contracts.py::StageContractsTest.test_photo_texture_blocks_only_after_shape_and_ink_pass` 验证只有形态和黑灰通过后才由 `photo_texture` 阻塞。
- [x] 更新所有 prompt、report、UI 文案中的旧 stage 名引用；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_authority.py::StageAuthorityTest.test_runtime_prompts_declare_only_the_five_public_stages` 验证 prompt 只声明五个公开 stage，`test_prompt_payload_and_reports_expose_only_five_stage_ids` 验证 prompt/report payload 的 `stage_id`、`blocking_stage`、`stage_order` 只包含五阶段，`test_runtime_prompts_cli_and_web_copy_do_not_expose_old_concerns_as_gates` 验证 runtime prompt、CLI、Web 和 run artifact 文案不暴露旧诊断 concern 名作为 gate。

### C. 全局硬约束

- [x] 输出尺寸与原图一致必须有 fixture 或 hard report 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_hard_boundary.py::HardBoundaryTest.test_size_mismatch_fails_and_records_sizes` 验证 hard report 记录 `original_size`、`candidate_size`、`size_match=false` 并失败。
- [x] ROI 外像素不变必须有 fixture 或 hard report 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_hard_boundary.py::HardBoundaryTest.test_inside_roi_change_passes_with_zero_outside_and_border_diff` 和 `test_outside_roi_change_fails_and_records_pixel_count` 验证 hard report 记录 `outside_roi_changed_pixels`，ROI 外变化会失败。
- [x] 图片边缘像素不变必须有 fixture 或 hard report 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_hard_boundary.py::HardBoundaryTest.test_border_change_fails_and_records_pixel_count` 验证 hard report 记录 `border_changed_pixels`，边缘变化会失败。
- [x] protected text 不变必须有 fixture 或 hard report 断言，且保护范围不按方向特判；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_hard_boundary.py::HardBoundaryTest.test_protected_text_change_fails_and_records_diff_for_manual_and_auto_regions` 验证 hard report 记录 `protected_changed_pixels`，manual/auto 类 protected box 变化都会失败；`tests/test_roi_locator_slots.py::RoiLocatorSlotTest.test_target_roi_protected_guard_is_direction_independent` 验证 protected text 在目标 ROI 的左、右、上、下任意方向发生交叠都会生成 `target_roi_overlaps_protected_text` 并进入 `protected_text_guard`。
- [x] 增加“目标 ROI 覆盖完整旧字”独立验收：旧字核心、灰边、底部和倾斜外溢都必须在 source slot 或 cleanup mask 内；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_quality_schema_records_counts_coverage_overlap_and_masks` 验证 `old_text_coverage_report` 稳定记录 core/gray/bottom/tilt/cleanup mask 且逐槽通过，`test_bottom_overflow_fails_slot_quality_before_candidate_generation` 和 `test_tilt_overflow_fails_slot_quality_before_candidate_generation` 验证普通旧槽位底部/倾斜外溢会使 `old_text_coverage_report.pass=false`，`test_shorter_replacement_cleanup_slot_covers_right_bottom_overflow` 验证多余旧槽位外溢由 cleanup mask 覆盖。
- [x] 增加“新字不能覆盖任何未修改内容”独立验收：字数增加和 ROI 扩展时必须记录 `right_boundary`、protected box 距离和最小安全间距，同时最终目标 ROI 必须对所有方向的 protected boxes 做交叠门禁；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_longer_replacement_right_boundary_blocks_protected_text_collision` 验证字数增加 fixture 的 `right_boundary` 记录 `roi_right_edge`、`minimum_safe_gap_px`、`protected_gap_px`、`protected_distances_px`、`protected_right_boxes`、`available_right_px` 和 `estimated_extra_width`，并在右侧 protected text 安全间距不足时生成 `right_boundary_too_close_to_protected_text`；`tests/test_roi_locator_slots.py::RoiLocatorSlotTest.test_target_roi_protected_guard_is_direction_independent` 验证 `target_roi_overlaps_protected_text` 覆盖上、下、左、右交叠。

### D. 方向、字段和旧值 ROI

- [x] 指令解析必须输出 `field`、`old_value`、`new_value`、解析置信和失败原因；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_instruction_parsing.py::InstructionParsingTest.test_parse_name_date_age_and_manual_roi_instruction_fields` 覆盖姓名、日期、年龄、手动 ROI 无旧值输入，`test_empty_instruction_reports_failure_reason` 覆盖失败原因，`test_process_cli_json_summary_includes_instruction_details` 验证 CLI JSON summary 暴露 `instruction_details`。
- [x] 自动方向选择不能只看整页方向；必须同时记录目标字段质量、旧值定位质量和最终方向理由；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_auto_roi_evidence.py::AutoRoiEvidenceTest.test_auto_orientation_records_field_and_old_value_quality_for_each_attempt` 验证每个方向 attempt 写入 `direction_quality`、`target_field_quality`、`old_value_location_quality`、`selection_basis` 和 `final_direction_reason`。
- [x] 每个自动 ROI 任务必须同时记录 `search_roi` 和 `edit_roi`，并输出标注图；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_auto_roi_artifacts.py` 验证 `auto_roi_evidence_payload` 同时记录 search/edit ROI 且 overlay 图片可保存，`tests/test_run_artifacts.py::RunArtifactsTest.test_result_audit_payload_strips_data_urls_but_keeps_stage_artifact_fields` 验证 `stage_evidence.auto_roi` 保留 search/edit ROI 和 overlay path。
- [x] `search_roi` 可以覆盖字段锚点和后续保护文本，`edit_roi` 必须收缩到旧值槽位和必要空白；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_auto_roi_evidence.py::AutoRoiEvidenceTest.test_auto_region_evidence_records_search_edit_field_and_old_value_quality` 和 `tests/test_auto_roi_artifacts.py::AutoRoiArtifactsTest.test_auto_roi_evidence_payload_records_search_and_edit_roi_contract` 验证 `search_area >= edit_area`、`edit_within_search_roi=true`、search 可与 protected/context overlap、`edit_protected_overlap_pixels=0`。
- [x] 找不到字段或旧值时必须立即失败并保留 rejected 产物；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_failure_artifacts.py::FailureArtifactsTest.test_auto_roi_failure_preserves_rejected_artifacts_and_result_evidence` 验证自动 ROI 找不到旧值时 `ok=false`、`accepted=false`、`applied=false`、`candidates=[]`，并保留 rejected input、failure report、`result.json` stage evidence 和 `progress.jsonl` 的 `image_failed`。
- [x] 自动 ROI 通用路径必须覆盖姓名、日期、年龄、数字编号和手动 ROI fallback；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_auto_roi_field_coverage.py::AutoRoiFieldCoverageTest.test_auto_roi_common_path_covers_name_date_age_and_number_fields` 使用组件 fixture 真实调用 `auto_select_regions_for_instruction` 覆盖 `name`、`date`、`age`、`number` 字段并验证 search/edit ROI 契约，`test_manual_roi_fallback_skips_auto_roi_selection` 通过 `process_payload` 验证传入手动 rect 时不调用自动 ROI，直接进入 region processing。

### E. 旧槽位完整性门禁

- [x] `slot_quality_report` 必须有稳定 schema 和 fixture 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_quality_schema_records_counts_coverage_overlap_and_masks` 验证 `source_count`、`target_count`、coverage schema、overlap report、right boundary 入口和 cleanup mask schema。
- [x] 逐字检查旧值字符数与槽位数匹配；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_count_mismatch_fails_with_source_and_target_counts` 验证 `source_count`、`target_count`、`expected_count`、`actual_count` 和 `slot_count_too_low`。
- [x] 逐槽检查核心笔画覆盖；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_quality_schema_records_counts_coverage_overlap_and_masks` 验证每个 slot 输出 `core_pixels` 和 `coverage.core_coverage`。
- [x] 逐槽检查灰边覆盖；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_quality_schema_records_counts_coverage_overlap_and_masks` 验证每个 slot 输出 `gray_edge_pixels` 和 `coverage.gray_edge_coverage`。
- [x] 逐槽检查底部覆盖；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_bottom_overflow_fails_slot_quality_before_candidate_generation` 验证底部暗像素外溢会生成 `slot_bottom_overflow` 并使 `slot_quality_report.pass=false`，`tests/test_slot_quality_gate.py::SlotQualityGateTest.test_failed_slot_quality_stops_before_candidate_generation` 验证该失败会在候选生成前早停。
- [x] 逐槽检查倾斜外溢覆盖；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_tilt_overflow_fails_slot_quality_before_candidate_generation` 验证普通旧槽位侧向外溢生成 `slot_tilt_overflow`、`tilt_coverage < 1.0` 且 `old_text_coverage_report.pass=false`，`test_shorter_replacement_cleanup_slot_covers_right_bottom_overflow` 验证多余旧槽位右下角外溢进入 cleanup mask。
- [x] 检查槽位未混入字段标签、冒号或前置文本；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_overlapping_left_label_is_reported_separately` 验证左侧 label/protected box 侵入旧字槽位会写入 `overlap_report.label_overlap_pixels` 并阻塞。
- [x] 检查槽位未混入后续未修改文本；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_slot_overlapping_protected_text_is_a_gate_failure` 验证 protected overlap 写入 `overlap_report.protected_overlap_pixels` 并以 `slot_overlaps_protected_text` 阻塞。
- [x] 检查最后一个旧字不会被误判成 protected text；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_shorter_replacement_cleanup_slot_covers_right_bottom_overflow` 验证字数减少时最后一个多余旧槽位及右下角外溢计入 `cleanup_mask_report` 和 `old_source_cleanup_overlap_pixels`，不会生成 `slot_overlaps_protected_text`。
- [x] 字数减少时，多余旧槽位必须进入前置清除区域；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_shorter_replacement_reports_extra_source_cleanup_mask` 验证 `extra_source_slots_for_cleanup`、`extra_source_cleanup_span` 和 `cleanup_mask_report`。
- [x] 字数增加时，右边界必须受后续 protected text 限制；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality.py::SlotQualityTest.test_longer_replacement_right_boundary_blocks_protected_text_collision` 验证 `right_boundary.available_right_px`、`protected_gap_px`、`limited_by_protected_text` 和 `right_boundary_too_close_to_protected_text`。
- [x] 旧槽位门禁失败必须阻塞候选生成或最终验收；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_slot_quality_gate.py::SlotQualityGateTest.test_failed_slot_quality_stops_before_candidate_generation` 验证 `slot_quality_report.pass=false` 时 `candidates=[]`、`accepted=false`、`applied=false`、`final_blocking_stage=hard_boundary`，并保留 `slot_quality_rejected_compare.png`。

### F. 放置策略选择

- [x] `placement_strategy` 和选择原因必须有 schema 和多场景 fixture 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_strategy_reason_is_declared_for_core_scenarios` 覆盖同字数 CJK、字数增加、字数减少、日期/年龄类非 CJK、手动 ROI 无旧值策略和 reason，`test_placement_report_schema_records_conditions_constraints_errors_and_pass` 验证 report schema 记录条件、约束、实际误差和 pass。
- [x] 同字数 CJK 且字形变化小：必须验证 `top_left_anchor` 或等价策略，约束中心误差、字距、基线；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_same_length_small_cjk_uses_top_left_with_center_spacing_and_baseline_constraints` 使用同字数小变化 fixture，验证 `shape_change_large=false` 时选择 `top_left_anchor`，并记录 `max_char_center_dx`、`max_char_spacing_delta`、`baseline_dy` 和 `char_spacing_delta`。
- [x] 同字数 CJK 且字形变化大：必须验证 `center_primary`，约束左边界、基线、字距；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_same_length_large_cjk_uses_center_with_left_baseline_and_spacing_constraints` 使用同字数大变化 fixture，验证 `shape_change_large=true` 时选择 `center_primary`，并记录 `left_boundary_dx`、`baseline_dy` 和 `char_spacing_delta`。
- [x] 字数减少：目标字按旧值整体跨度排布，并清理多余旧槽位；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_shorter_replacement_uses_old_span_and_requires_extra_slot_cleanup` 使用 3 字变 2 字 fixture，验证 `left_anchor_span`、`span_width_delta`、`char_spacing_delta`、`baseline_dy` 和 `cleanup_extra_source_slots_required`。
- [x] 字数增加：旧值起点锚定，已有旧字槽位不压缩，新增字从旧值最右侧继续追加，且不覆盖任何 protected text；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_longer_replacement_left_anchors_extends_right_and_guards_protected_text` 使用 2 字变 3 字 fixture，验证 `left_anchor_span`、`draw_mode=line_chars`、`source_slot_count=2`、`target_slot_count=3`、`longer_text_appends_slots=true`、`protected_text_overlap_pixels=0` 和 `right_boundary_pass=true`；`tests/test_shape_scoring.py::ShapeScoringTest.test_longer_replacement_appends_target_slots_without_compressing_old_slots` 验证第 3 个目标槽位从旧字最右侧向右追加，前两个旧槽位坐标保持不变；`tests/test_shape_scoring.py::ShapeScoringTest.test_longer_replacement_gray_metrics_reference_old_ink_not_empty_append_area` 验证新增字墨色参考旧字槽位而不是右侧空白背景；`tests/test_roi_locator_slots.py::RoiLocatorSlotTest.test_labeled_cjk_value_strategy_precedes_unlabeled_fallback` 验证有标签/冒号时优先从旧值槽位起点开始，不能让无标签兜底把字段标签吞进旧值槽位。
- [x] 字数变化或中心绘制时，仍必须按旧文字原位置和周围同一行未修改文字约束行基线；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_row_baseline_metrics_flags_centered_text_that_sits_too_low` 验证 `row_baseline_metrics` 在中心绘制模式下输出 `candidate_box`、`source_slot_boxes`、`same_row_protected_boxes`、`reference_center_y` 和 `row_baseline_center_y_too_low`，`test_row_baseline_metrics_accepts_centered_text_on_old_row` 验证贴近旧行位置时不报错，`test_row_baseline_issues_are_text_shape_stage_issues` 验证该问题归入 `text_shape`。
- [x] 数字、日期、编号：左对齐和基线优先，保持数字节奏和字段宽度；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_numeric_date_and_age_use_left_baseline_rhythm_and_field_width_constraints` 覆盖日期和年龄 fixture，验证 `baseline_numeric`、`left_boundary_dx`、`baseline_dy`、`rhythm_delta` 和 `field_width_delta`。
- [x] 手动 ROI 且无旧值：使用保守居中或左对齐 fallback，并降低自动验收置信；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_manual_roi_without_source_uses_conservative_fallback_and_reduced_acceptance_confidence` 验证 `manual_fallback`、`manual_roi_conservative`、允许 center/left 对齐，并记录 `auto_acceptance_confidence=reduced` 和 `auto_acceptance_confidence_cap=0.45`。
- [x] 每个放置策略必须在 `result.json` 写入使用条件、关键约束、实际误差和是否通过；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_placement_strategy.py::PlacementStrategyTest.test_placement_report_schema_records_conditions_constraints_errors_and_pass` 验证 `placement_strategy_report` schema，`test_result_audit_preserves_placement_strategy_report` 验证 result audit 保留 strategy、reason、conditions、constraints、actual_errors 和 pass。

### G. 单字形态变化检测

- [x] `bbox_width_delta_ratio`、`bbox_height_delta_ratio`、`centroid_dx/dy`、`ink_area_ratio` 必须有 schema 和 fixture 断言；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证每个 changed char 输出这些字段。
- [x] 每个 changed char 都必须生成旧槽位画像和新字候选画像；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证 `shape_change_report.changed_chars[*]` 含 `source_image_metrics` 和 `target_image_metrics`。
- [x] 增加 `row_projection_distance`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证字段存在，并以 `row_projection_distance_large` 参与 `shape_change_large`。
- [x] 增加 `col_projection_distance`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证字段存在，并以 `col_projection_distance_large` 参与 `shape_change_large`。
- [x] 增加 `margin_distribution_delta`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证字段存在，并以 `margin_distribution_delta_large` 参与 `shape_change_large`。
- [x] 动态阈值必须来自旧槽位高度、邻字稳定性和字体候选分布；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_dynamic_thresholds_record_slot_neighbor_and_font_distribution_sources` 验证每个动态阈值写入 `threshold_context.old_slot_height`、`threshold_context.neighbor_stability`、`threshold_context.font_candidate_distribution` 和 `source_components=["old_slot_height","neighbor_stability","font_candidate_distribution"]`。
- [x] 固定数字阈值只能作为第一版保守起点，并必须写入报告；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_changed_char_schema_records_bbox_projection_margin_and_threshold_sources` 验证 `thresholds.*.threshold_source=dynamic`，同时每个 threshold 写入动态来源上下文；`src/roi_image_edit/shape_metrics.py::default_shape_thresholds` 只作为 `dynamic_shape_thresholds` 的保守 base。
- [x] 禁止用语义字表判断“单字变化大”；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_change_report.py::ShapeChangeReportTest.test_shape_change_large_uses_geometry_not_character_table` 验证不同字符但几何一致时 `shape_change_large=false`，并验证报告声明 `large_change_decision=dynamic_geometry_thresholds`、`semantic_character_table_used=false`。

### H. 字体形态联合搜索

- [x] `text_shape` 阻塞时生成 shape candidate grid，且 grid 只包含字体、字号、放置、`text_dx/text_dy`、`char_offsets`、stroke body、shear；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_candidate_grid.py::ShapeCandidateGridTest.test_text_shape_grid_reports_budget_and_allowed_delta_keys` 验证 `text_shape_reset_candidate_grid` 只允许字体、字号、text dx/dy、char offsets 和 stroke-body 参数变化，禁止 opacity、blur、mask/inpaint、photo_warp、edge_breakup、photo_noise、jpeg_quality 变化，并声明 shear 来源为旧槽位/邻字继承；`test_processing_service_preserves_shape_grid_report_in_revision_rounds` 验证 revision round 写入 `shape_candidate_grid`。
- [x] 首轮本地候选必须包含 `text_dy` 小步搜索，避免中心绘制模式检测到行基线问题却没有可选候选；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_candidate_grid.py::ShapeCandidateGridTest.test_initial_candidate_grid_includes_row_baseline_y_offsets` 验证 iteration 0 候选包含 `text_dy=-2/-1/+1`。
- [x] 形态排序必须包含字高、字宽、字距、基线；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `shape_score_breakdown.components.height_width_spacing_baseline` 输出 width、height、gap 和 baseline 明细。
- [x] 形态排序必须包含单字中心与旧槽位中心误差；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `center_error.max_abs_centroid_dx/dy`。
- [x] 形态排序必须包含左边界和右边界误差；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `boundary_error.max_abs_left_delta_px` 和 `max_abs_right_delta_px`。
- [x] 形态排序必须包含笔画面积和复杂度修正后的体量；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `ink_area_complexity.text_complexity_ratio` 和 complexity-adjusted ink delta。
- [x] 形态排序必须包含姿态继承误差；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `pose_inheritance.max_abs_shear_error`。
- [x] 形态排序必须包含 protected text 距离；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `protected_distance.min_horizontal_gap_px`。
- [x] 形态排序必须包含字体风格分数和可渲染字符检查；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_scoring.py::ShapeScoringTest.test_breakdown_records_all_shape_ranking_components` 验证 `font_style.font_style_score_ratio`、`font_family_score_ratio` 和 `renderable_text_check.missing_chars`。
- [x] 形态没通过时，`blur`、`noise`、`jpeg_quality`、背景融合不能成为主修复方向；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_text_shape_stage_rejects_photo_or_background_primary_patches`。
- [x] `text_shape` 仍优先保证字体、字号、槽位、基线、字距、笔画粗细和局部姿态，但形态候选不能明显恶化墨色；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_revision_stage_progression.py::RevisionStageProgressionTest.test_text_shape_ink_guard_is_selectable_only_when_shape_does_not_regress` 验证 `ink_guard` 只有在 `text_shape` 严重度不回退且 `ink_gray_balance` 严重度下降时才可选，`test_text_shape_ink_guard_rejects_shape_regression_even_if_ink_improves` 验证即使降黑有效也不能牺牲形态阶段。

### I. 黑灰比例搜索

- [x] `ink_gray_balance` 只在形态 top candidates 上执行；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_ink_gray_grid_reports_budget_parent_and_allowed_delta_keys` 验证 ink grid 报告 `parent_shape_contract`，要求 `text_shape_passed_before_ink_gray`，所有 ink 候选都记录同一个 `parent_shape_candidate_id`；`test_parent_shape_candidate_survives_multiple_ink_gray_rounds` 验证多轮 ink 调整时 `parent_candidate_id` 可以变成上一轮 ink candidate，但 `parent_shape_candidate_id` 必须保持已通过形态阶段的 candidate；`test_layered_search_report_preserves_ink_parent_shape_trace` 验证分层搜索总报告保留 `parent_shape_trace`，使 ink candidate 能追溯到已通过形态阶段的 parent candidate。
- [x] `text_shape` 阻塞且黑芯严重过量时必须生成单独的 `ink_guard` 候选族，不把降黑归入 `text_shape` 主修复；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_text_shape_excess_black_enables_separate_ink_guard_grid` 验证 `optimization_step=ink_guard`、`guards_stage=text_shape`、不改变字体/字号/位置/字槽，`test_text_shape_without_excess_black_does_not_enable_ink_guard` 验证无严重过黑时不会生成该候选族。
- [x] 黑灰报告必须分开记录 `<55`、`<70`、`70-120`、`120-165`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_strict_visual_metrics.py::StrictVisualMetricsTest.test_ink_gray_report_records_core_dark_body_and_outer_gray_bands` 验证 `strict_visual_metrics.bands` 分别记录 `<55`、`<70`、`70-120`、`120-165` 的 old/new/delta 字段。
- [x] 核心太黑时，必须生成降低 `opacity`、`core_ink_gain` 或 `core_darken_strength` 的候选；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_ink_gray_dispatch_generates_opposite_directions_for_black_core_and_light_core` 验证 `changed_char_core_too_black` 进入 `ink_gray_balance` patcher，并生成负向 `opacity_delta`、`core_ink_gain_delta` 或 `core_darken_strength_delta`。
- [x] 核心不足但灰边多时，不能继续加 blur 或扩大灰边，必须恢复核心密度并收紧外灰；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_core_light_with_outer_halo_recovers_core_without_expanding_gray` 验证 grid 策略为 `recover_core_density_and_trim_outer_gray`，候选恢复核心且不增加 blur/noise/edge breakup；`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_ink_gray_core_light_with_outer_halo_rejects_blur_and_recovers_core` 验证 patcher 拒绝 photo/blur 方向，并只保留核心恢复与外灰收紧补丁。
- [x] 只剩近阈值核心偏亮时，必须生成更细粒度的核心微调候选，而不是继续用 0.02 级别的大步长震荡；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_near_threshold_core_light_enables_micro_tuning` 验证仅 `core_mean_gray_too_light` 且 gap 小于 `0.75` 时启用 `core_only_micro_recovery`，`test_near_threshold_micro_grid_preserves_shape_and_prepends_fine_core_candidates` 验证微调候选排在 ink grid 前面，只小步改变核心黑灰参数并保留字体、字号、位置、模糊、mask 和背景参数，`test_non_near_core_light_does_not_enable_micro_tuning` 验证非近阈值不会触发。
- [x] 通过当前阻塞阶段但进入下一阶段失败的候选必须被视为阶段推进候选，继续下一轮修复；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_revision_stage_progression.py::RevisionStageProgressionTest.test_candidate_that_passes_current_stage_and_blocks_later_progresses` 验证 `ink_gray_balance` pass 且 blocking stage 变成 `background_cleanup` 时返回推进，`test_revision_loop_allows_current_stage_progression_as_selectable` 验证 revision loop 将 `progresses_past_current_stage` 纳入可选候选条件。
- [x] 旧字和邻字指标冲突时，必须记录仲裁，并优先同一行邻字作为风格上限；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_reference_profile_arbitration.py::ReferenceProfileArbitrationTest.test_same_row_neighbor_wins_when_source_and_neighbor_ink_conflict` 验证 `reference_profile.dynamic_ink.arbitration` 记录冲突并选择 `same_row_neighbor`，`test_source_only_arbitration_when_no_same_row_neighbor_exists` 验证没有同一行邻字时才使用 `source_only_no_same_row_neighbor`。
- [x] 黑灰阶段不能改变已经通过的字体、槽位和基线，除非重新回到 `text_shape`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_ink_gray_grid_reports_budget_parent_and_allowed_delta_keys` 验证 `ink_gray_candidate_grid` 记录 `parent_shape_candidate_id`、`preserved_shape_keys`、`shape_key_changes_require_stage=text_shape`，且每个 ink 候选保持 font、字号、`text_dx/text_dy`、`char_offsets`、mask/background/photo 参数不变。

### J. 照片质感搜索

- [x] `photo_texture` 只能在 `text_shape` 和 `ink_gray_balance` 通过后执行；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_photo_texture_blocks_only_after_shape_and_ink_pass` 验证同时存在形态/黑灰/照片质感问题时先阻塞 `text_shape`，形态通过后先阻塞 `ink_gray_balance`，只有前两者通过后才阻塞 `photo_texture`。
- [x] 可调参数必须限定为小幅 blur、edge breakup、局部噪声、压缩质感、轻微 alpha 退化、局部残差回填；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_photo_texture_patcher_declares_only_small_texture_parameter_family` 验证 `photo_texture` stage patcher 只允许 `blur_delta`、`alpha_contrast_delta`、`photo_warp_delta`、`edge_breakup_delta`、`photo_noise_delta`、`jpeg_quality_delta`，不允许 `opacity_delta`、`stroke_opacity_delta` 或 `font_size_delta`；`tests/test_photo_texture_candidate_grid.py::PhotoTextureCandidateGridTest.test_photo_texture_grid_reports_budget_and_allowed_delta_keys` 验证 photo grid 记录 `alpha_adjustment_scope`、`residual_retexture_keys`，并限制每个候选的小幅调参范围。
- [x] 目标必须是匹配原图拍照/扫描质感，不是把字弄糊；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_photo_texture_validation.py::PhotoTextureValidationTest.test_photo_texture_axes_report_records_texture_not_just_blur` 验证 `photo_texture_metrics.texture_axes` 同时记录 `sharpness`、`breakup`、`noise`、`compression`，并声明目标为匹配原图照片/扫描质感。
- [x] 照片质感不能破坏已通过的黑灰和形态指标；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_photo_texture_candidate_grid.py::PhotoTextureCandidateGridTest.test_photo_texture_candidate_records_prior_shape_or_ink_regression` 验证 `prior_stage_regression` 记录 photo 候选对 `hard_boundary`、`text_shape`、`ink_gray_balance` 的 before/after severity 和 pass 状态，形态或黑灰回退会记录 regression；`run_region_vision_checks` 只把 `prior_stage_regression.pass=true` 的候选纳入可选集合。
- [x] 文字过清晰、过干净、过糊、边缘无断裂必须进入 `photo_texture` 问题报告；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_photo_texture_validation.py` 验证 `photo_texture_too_sharp`、`photo_texture_too_clean`、`photo_texture_too_blurry`、`photo_texture_edge_breakup_missing` 和 `photo_texture_not_applied` issue type，并验证无断裂问题会贡献 `photo_texture` stage severity。

### K. 背景处理拆分

- [x] 前置清除必须删除旧值槽位内旧字核心和灰边；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_source_slot_precleanup_removes_old_core_and_gray_edge` 验证 `source_slot_precleanup_report` 的 `source_slots_excluding_new_text_alpha` mask、旧核心/灰边像素和候选残留 ratio。
- [x] 旧槽位灰边残留必须按候选局部背景动态判断，不能把拍照灰底或新字低 alpha 抗锯齿边缘误判为旧字残留；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_source_slot_precleanup_does_not_count_clean_photo_gray_as_old_residue` 验证干净灰底不触发 `source_slot_gray_edge_residue`，`test_source_slot_precleanup_still_blocks_true_gray_edge_residue_on_photo_gray` 验证真实旧灰边仍阻塞。
- [x] 字数减少时，前置清除必须覆盖多余旧槽位；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_shorter_replacement_requires_extra_source_cleanup_coverage` 验证 3 字变 2 字时 `extra_source_cleanup_coverage_report` 记录 expected extra slot、cleanup crop 和 slot quality cleanup mask box。
- [x] 前置清除失败必须阻塞候选生成或最终验收；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_source_slot_precleanup_residual_blocks_background_cleanup_stage` 验证旧字核心/灰边残留 issue 归入 `background_cleanup` blocking stage。
- [x] 后置融合只围绕最终文字形态做局部融合；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_post_blend_scope_stays_inside_target_roi_and_avoids_protected_text` 验证 `post_blend_report.scope.scope_box == target_roi`、`outside_target_roi_pixels=0`、`protected_overlap_pixels=0`。
- [x] 后置融合必须分别报告补丁感、发白、发暗、平滑涂抹、纹理断裂和 ROI 边缘接缝；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_post_blend_reports_patch_white_dark_smooth_texture_and_roi_edge_axes` 验证 `artifact_axes` 包含 `patch_visible`、`white_ghost`、`dark_shadow`、`smooth_smear`、`texture_break`、`roi_edge_seam` 并生成对应 issue。
- [x] 后置融合不能掩盖旧槽位没清干净；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_background_cleanup.py::BackgroundCleanupTest.test_pre_cleanup_failure_takes_priority_over_post_blend_naturalness` 验证 `background_cleanup_stage_report.priority_order=["pre_cleanup","post_blend"]`，且 `pre_cleanup.pass=false` 时 `post_blend_can_deliver=false`、blocking step 为 `pre_cleanup`。

### L. 分层联合优化和搜索预算

- [x] 禁止全量笛卡尔积搜索；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_layered_candidate_search.py::LayeredCandidateSearchTest.test_layered_report_records_stage_budgets_without_cross_stage_cartesian_total` 验证 `layered_candidate_search` 只记录各 stage 的 raw budget、retained、pruned，声明 `cross_stage_cartesian_search=false`，且不输出 `total_raw_candidate_budget`、`cross_stage_cartesian_budget` 或 `single_full_combination_total`；`test_processing_service_writes_layered_report_in_revision_rounds` 验证 revision round 写入该报告。
- [x] Stage A shape search 本地候选预算为 300-1500，剪枝后保留 top 20-50；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_candidate_grid.py::ShapeCandidateGridTest.test_text_shape_grid_reports_budget_and_allowed_delta_keys` 验证 `shape_candidate_grid.budget.raw_candidate_budget` 在 300-1500 内、`retained_count=48`、`pruned_count>0`。
- [x] Stage B ink-gray search 本地候选预算为 100-800，剪枝后保留 top 8-20；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_ink_gray_grid_reports_budget_parent_and_allowed_delta_keys` 验证 `ink_gray_candidate_grid.budget.raw_candidate_budget` 在 100-800 内、`retained_count` 在 8-20 内、`pruned_count>0`，且候选只改变 `ink_gray_balance` 允许参数；`test_processing_service_preserves_ink_gray_grid_report_in_revision_rounds` 验证 revision round 写入 `ink_gray_candidate_grid`。
- [x] Stage C photo texture search 本地候选预算为 30-200，剪枝后保留 top 3-8；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_photo_texture_candidate_grid.py::PhotoTextureCandidateGridTest.test_photo_texture_grid_reports_budget_and_allowed_delta_keys` 验证 `photo_texture_candidate_grid.budget.raw_candidate_budget` 在 30-200 内、`retained_count` 在 3-8 内、`pruned_count>0`，且候选只改变 `photo_texture` 允许参数；`test_processing_service_preserves_photo_texture_grid_report_in_revision_rounds` 验证 revision round 写入 `photo_texture_candidate_grid`。
- [x] Stage D vision final check 只看 top 3-8；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_run_artifacts.py::RunArtifactsTest.test_vision_candidate_request_records_limit_and_stage_context` 验证请求值低于 3 会提升到 `vision_candidate_limit=3`、高于 8 会收口到 `vision_candidate_limit=8`，`tests/test_vision_candidate_boundary.py::VisionCandidateBoundaryTest.test_region_vision_request_uses_top_candidates_with_stage_context` 验证视觉 prompt 实际只收到规范化后的 top candidates。
- [x] 形态剪枝必须覆盖字高、中心、基线、字距、protected distance、字体风格、笔画体量、姿态继承；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_shape_candidate_grid.py::ShapeCandidateGridTest.test_text_shape_grid_reports_budget_and_allowed_delta_keys` 验证 `text_shape` grid 的 `prune_reason_contract.required_categories` 包含 `glyph_height`、`center_alignment`、`baseline_alignment`、`character_spacing`、`protected_distance`、`font_style`、`stroke_body`、`pose_inheritance`，每个 retained candidate audit 只能使用这些 reason categories。
- [x] 黑灰剪枝必须覆盖真黑核心、深色核心、外灰边、中灰笔画、复杂度修正；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_ink_gray_candidate_grid.py::InkGrayCandidateGridTest.test_core_light_with_outer_halo_recovers_core_without_expanding_gray` 验证 `ink_gray_balance` grid 的 `prune_reason_contract.required_categories` 包含 `true_black_core`、`deep_core_density`、`outer_gray_edge`、`mid_gray_body`、`complexity_adjustment`，并验证复杂度修正来源为 `reference_profile` 和 `target_source_complexity_ratio`。
- [x] 照片质感剪枝必须覆盖过锐、过糊、无断裂、背景平滑、白影/暗影/旧残留、ROI 梯度断裂；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_photo_texture_candidate_grid.py::PhotoTextureCandidateGridTest.test_photo_texture_grid_reports_budget_and_allowed_delta_keys` 验证 `photo_texture` grid 的 `prune_reason_contract.required_categories` 包含 `over_sharp`、`over_blurry`、`missing_edge_breakup`、`background_too_smooth`、`white_or_shadow_ghost`、`old_residual`、`roi_gradient_break`，并验证白影/暗影来源为 background ghost issue types。

### M. Stage patcher 迁移边界

- [x] stage patch filter 已能按当前 blocking stage 接受/拒绝补丁族；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_text_shape_stage_rejects_photo_or_background_primary_patches`。
- [x] 每个 stage patcher 必须声明 primary stage、allowed keys、blocked keys；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_stage_patchers_declare_primary_allowed_and_blocked_keys`。
- [x] patcher 输出不能包含未声明参数；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_stage_patcher_outputs_do_not_contain_undeclared_keys`。
- [x] 跨 stage patch 必须被拒绝或声明主阶段、次级影响和不破坏前置阶段的依据；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_filter_report_declares_secondary_impacts_and_rejects_cross_stage_primary`。
- [x] 新增 patch 必须进入某个 stage patcher，不能散落在全局候选生成函数；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_revision_patch_entrypoints_are_registered_or_explicit_fallback` 和 `tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_runtime_code_uses_dispatcher_not_concrete_stage_patchers`。
- [x] 旧入口只能调用 stage dispatcher，stage dispatcher 不能反向调用旧全局混合补丁；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_legacy_revision_entry_delegates_to_dispatcher`。
- [x] 临时双轨只允许用于同输入新旧结果差异验证，不能作为长期交付路径；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_no_runtime_switch_points_to_old_mixed_revision_path` 验证 runtime 代码没有指向旧混合 revision/patch 路径的 CLI/env 开关，也没有在 `stage_patchers.py` 外直接调用 `final_acceptance_patches`。
- [x] 每个阶段迁移必须包含 detector、patcher、allowed/blocked 参数、失败用例、通过用例和 stage evidence；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_migration_contract.py::StageMigrationContractTest.test_every_stage_migration_declares_detector_patcher_and_evidence` 验证 `stage_migration_contract_report()` 为五个 stage 全部输出 detector、patcher、allowed/blocked patch keys、blocking failure case、pass case 和 model-facing stage evidence。

### N. Profile 验收

- [x] profile registry 已公开 `photo_scan`、`clean_digital`、`low_res_thumbnail`、`manual_roi_quick`，且 `clean_digital` 当前禁用 `photo_texture`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_profile_contracts_cover_current_profiles`。
- [x] `photo_scan` 必须启用姿态和照片质感，且视觉模型不能在 stroke/shape 失败时 deliver；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_photo_scan_profile_enables_pose_texture_and_blocks_visual_deliver_on_shape` 验证 `photo_scan` 启用 `enable_pose`、`enable_photo_texture`、`enable_photo_warp`，并验证本地 `apply_local_acceptance_gate` 会把 shape/stroke 失败下的视觉 `deliver` 改成 `revise`。
- [x] `clean_digital` 不启用 `photo_texture`，不鼓励 `photo_warp`，边缘应更干净；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_clean_digital_profile_disables_photo_texture_and_photo_warp` 验证 `clean_digital` 从 stage order 中移除 `photo_texture`、`enable_photo_warp=false`、`edge_policy=clean_edges_no_photo_warp`，且 prompt `profile_constraints` 暴露这些限制。
- [x] `low_res_thumbnail` 更重视字体结构和笔画体量，并要求视觉验收看放大图；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_low_res_thumbnail_profile_prioritizes_shape_and_uses_magnified_context` 验证 `shape_priority` 包含 `font_family_similarity`、`stroke_body_weight`、`slot_geometry`、`ink_gray_density`，且 prompt `profile_constraints.vision_context_scale=magnified`；`test_profile_shape_priorities_do_not_reintroduce_old_stage_ids` 验证 profile payload 不重新暴露旧 public stage/concern id。
- [x] `manual_roi_quick` 只做最少阶段，未通过时保留 rejected candidate，不自动做复杂照片质感；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_manual_roi_quick_profile_uses_minimal_stages_and_rejected_artifacts` 验证该 profile 只启用 `hard_boundary`、`text_shape`、`ink_gray_balance`，禁用 `photo_texture`/`photo_warp` 和 `background_cleanup`，并在 prompt `profile_constraints` 记录 `revision_complexity=minimal`、`preserve_rejected_candidate=true`。
- [x] 同一张图可以用不同 profile 运行，并在 `result.json` 记录不同 stage order 或启用阶段差异；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_same_image_profile_matrix_records_stage_order_difference` 用同一 image id 验证 `photo_scan` 与 `clean_digital` 的 result audit 记录不同 stage order，且 `clean_digital` 不启用 `photo_texture`。
- [x] 用户指定 profile 必须优先于自动 profile 建议；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py::StageProfilesTest.test_explicit_profile_overrides_auto_suggestion` 验证 resolver 选择显式 profile，`tests/test_stage_profiles.py::StageProfilesTest.test_explicit_profile_resolution_is_preserved_in_result_audit` 验证 result audit 使用用户指定 profile 并保留自动建议仅供参考。

### O. 视觉模型 prompt 和本地仲裁

- [x] prompt 输入 stage context、输出建议不能越过本地 stage filter 必须有 mock 或 fixture 验证；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_prompt_assets_require_stage_context_allowed_and_blocked_keys` 验证 prompt 资产包含 `stage_context`/`blocking_stage`/`blocked_patch_keys`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_forbidden_model_suggestion_is_rejected_and_audited` 验证 forbidden suggestion 被本地拒绝并写入 `model_suggestion_filter.attempt_records`。
- [x] prompt 必须先判断当前 `blocking_stage` 是否真实存在；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_prompt_assets_require_stage_context_allowed_and_blocked_keys` 验证 prompt 输出 schema 必含 `stage_assessment.blocking_stage_exists`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_model_stage_response_contract_records_current_stage_and_basis` 验证 response contract 记录该声明是否匹配本地阶段。
- [x] prompt 只能针对当前 `blocking_stage` 给建议；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_model_stage_response_contract_flags_cross_stage_suggestion` 验证跨阶段 `suggestion_target_stage` 会被标记为 `suggestion_targets_current_stage=false`，且 forbidden patch 仍由 `test_forbidden_model_suggestion_is_rejected_and_audited` 拒绝。
- [x] prompt 不能建议当前阶段禁止参数；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_prompt_assets_require_stage_context_allowed_and_blocked_keys` 验证 prompt 明确携带 forbidden 参数边界，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_forbidden_model_suggestion_is_rejected_and_audited` 验证 mock response 中 `mask_threshold_delta` 和 `opacity_delta` 在 `text_shape` 阶段被拒绝。
- [x] 动态 prompt 必须嵌入本次字段上下文，不能在模板里写死某个字段标签；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_prompt_context_fields.py::PromptContextFieldsTest.test_instruction_details_include_field_label_separator_and_protected_texts` 验证指令解析输出 `field_label_text`、`field_separator_text` 和 `protected_texts`，`test_processing_prompt_context_embeds_dynamic_field_context` 与 `test_iterative_vision_context_embeds_dynamic_field_context` 验证运行时 prompt context 写入这些字段且不出现姓名专用表达。
- [x] packaged prompts 不能锚定单一字段、单一字体风格或固定候选参数示例；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_no_hardcoded_special_cases.py::NoHardcodedSpecialCasesTest.test_packaged_prompts_do_not_anchor_to_single_field_or_fixed_candidate_values` 验证 prompt 资产不含固定字段标签、固定字号枚举、固定 opacity 二元示例或固定字体类别目标。
- [x] 模型 prompt 的格式化输入和输出 JSON 字段必须逐项登记并可检查；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_prompt_io_contracts.py::PromptIoContractsTest.test_web_candidate_and_final_prompts_contain_declared_formatted_inputs` 与 `test_cli_tuning_prompt_contains_declared_formatted_inputs` 验证 Web/CLI prompt 最终字符串包含声明的动态字段、ROI、slot、protected text 和 stage context，`test_every_prompt_output_json_field_has_a_declared_handling_status` 验证每个 packaged prompt 的 `输出 JSON` 字段都有 `execute`、`validate`、`record`、`ignore_model_claim` 或 `reserved_not_called` 处理状态，`tests/test_run_artifacts.py::RunArtifactsTest.test_external_artifact_schema_covers_result_progress_and_explanations` 验证 `external_artifact_schema_report()` 暴露 `prompt_io_contract`，其中 `best_opacity`/`best_blur` 这类保留专项字段被明确标记为未接入运行时调用。
- [x] 每次视觉模型请求必须保存运行时 prompt 审计产物，能证明最终 prompt 文本、prompt hash、图片 hash、输入字段存在性和响应 JSON 字段；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_vision_prompt_audit.py::VisionPromptAuditTest.test_write_vision_prompt_audit_records_prompt_text_hashes_images_and_response` 验证 `*_prompt_audit.json`、`*_user_prompt.txt` 和 `*_system_prompt.txt` 会记录 prompt 哈希、完整 prompt 文本路径、图片 sha256 与响应 top-level keys，`test_vision_client_call_json_writes_audit_for_successful_response` 验证 `VisionClient.call_json` 成功响应时自动写审计，`tests/test_run_artifacts.py::RunArtifactsTest.test_delivery_artifact_manifest_explains_rejected_region_with_stage_candidates` 验证 `vision_prompt_audits` 被 artifact manifest 索引。
- [x] 如果模型认为前置阶段已通过，必须说明依据；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_prompt_assets_require_stage_context_allowed_and_blocked_keys` 验证 prompt schema 必含 `basis`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_model_stage_response_contract_records_current_stage_and_basis` 验证 response contract 记录 `basis_present` 和 `schema_complete`。
- [x] 如果模型建议 deliver 但本地 `blocking_stage` 不为空，本地必须改为 revise；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_local_acceptance_gate.py::LocalAcceptanceGateTest.test_local_blocking_stage_overrides_visual_deliver`。
- [x] 视觉模型建议必须转成本地候选或记录不可转化原因；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_model_suggestions.py::ModelSuggestionsTest.test_unconvertible_model_suggestion_is_recorded_with_reason` 验证 unsupported parameter、缺少 delta、非对象 suggestion 都不会静默丢弃，而是写入 `model_suggestion_filter.attempt_records[*].rejection_reason`。

### P. 进度、UI 和失败产物

- [x] 失败也必须保留 rejected candidate、progress、result、stage evidence；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_failure_artifacts.py::FailureArtifactsTest.test_auto_roi_failure_preserves_rejected_artifacts_and_result_evidence` 验证自动 ROI 失败任务保留 rejected input、failure report、`final_is_rejected_candidate=true`、run directory 下的 `result.json` 和 `progress.jsonl`，且 `result.json` 保留 failure stage evidence、`progress.jsonl` 记录 `image_failed`。
- [x] CLI 每轮必须显示或输出 `round`、`profile`、`blocking_stage`、`reason`、`allowed_params`、`blocked_params`、`selected_optimization_step`、`shape_reset_count`、`ink_gray_count`、`ink_guard_count`、`photo_texture_count`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_cli_progress.py` 验证 `revision_round_candidates` 和 `revision_round_finished` 文本进度行包含这些字段，且 `processing_service.py` 在轮次事件中写入 `selected_optimization_step`。
- [x] Web 每轮必须展示 `blocking_stage`、当前阶段失败原因、本轮候选图、因前置阶段失败被拒绝的候选、最终 `accepted` 状态；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_web_progress_dom.py::WebProgressDomTest.test_progress_dom_exposes_stage_reason_candidates_and_accepted_state` 用 Node VM DOM 测试验证进度 DOM 输出 `blocking_stage`、`reason`、`candidates`、`accepted` 和 `data-blocking-stage`，`test_failed_result_dom_shows_rejected_input_and_final_accepted_state` 验证前置失败结果在右侧显示 rejected input、`accepted=false` 和失败 gate，`tests/test_web_boundary.py` 验证 Web 入口仍不承载图像处理或阶段策略。
- [x] 失败任务必须保留自动方向选择报告、search/edit ROI 标注图、slot quality report、shape top candidates、ink-gray top candidates、photo texture top candidates、final visual candidates、rejected final candidate；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_failure_artifacts.py::FailureArtifactsTest.test_rejected_auto_roi_task_preserves_required_artifact_family` 通过 mocked rejected auto ROI 任务验证 run directory 中存在 `auto_orientation_report`、`auto_roi_evidence_report`、`auto_roi_overlay`、`slot_quality_report`、`pre_candidate_gate_report`、`text_shape_top_*`、`ink_gray_balance_top_*`、`photo_texture_top_*`、`vision_candidate_sheet`、`vision_final_compare`、`selected_candidate`、`selected_compare` 和 `final_image` 并被 `artifact_manifest.json` 索引，`tests/test_slot_quality_gate.py::SlotQualityGateTest.test_failed_slot_quality_stops_before_candidate_generation` 验证真实 slot quality 失败路径写出 `slot_quality_report.json`、`pre_candidate_gate_report.json` 和 rejected compare。

### Q. 回归 Case A-D

- [x] Case A 字段旧文字修改为新文字：fixture、命令、预期 `blocking_stage`、核心密度阈值、外灰阈值和视觉不能覆盖本地失败条件必须写入本 checklist 或测试数据；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/fixtures/regression_cases/case_a_field_text_shape.json` 保存命令、`expected_report_fields`、核心密度阈值和外灰阈值，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_case_a_field_edit_blocks_shape_before_cleanup_and_visual_deliver` 验证 `stage_gate.blocking_stage=text_shape` 且视觉 deliver 会被本地改为 revise。
- [x] Case A 必须验证 `stroke_body`/笔画体量在 edge cleanup 前通过；验证方式是粗细不足样例不能 deliver；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_case_a_field_edit_blocks_shape_before_cleanup_and_visual_deliver` 验证 `ink_area_ratio_too_low` 使 `text_shape` 阻塞，`stroke_opacity_delta` 被接受而 `mask_threshold_delta` 与 `photo_noise_delta` 被拒绝，粗细不足不能靠 edge cleanup 或照片噪声交付。
- [x] Case B 字数减少：fixture、命令、多余旧槽位清理、后续文字不移动、旧残留阶段归因必须写入测试；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/fixtures/regression_cases/case_b_shorter_cleanup.json` 保存命令和 expected fields，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_case_b_shorter_replacement_cleans_extra_slots_and_keeps_residue_out_of_photo_texture` 验证 `cleanup_mask_report.enabled=true`、多余旧槽位进入清理、`protected_changed_pixels=0`、旧残留归因到 `background_cleanup` 而不是 `photo_texture`。
- [x] Case C 字数增加：fixture、命令、不覆盖后续字段、ROI 扩展受 protected text 限制、先过字体字距再调颜色必须写入测试；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/fixtures/regression_cases/case_c_longer_protected.json` 保存命令和 expected fields，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_case_c_longer_replacement_is_limited_by_protected_text_and_shape_before_color` 验证 `right_boundary.limited_by_protected_text=true`、protected gap 不足会失败，且 `text_shape` 阻塞时 `font_size_delta` 被接受、`opacity_delta` 被拒绝。
- [x] Case D 干净数字图：fixture、命令、`clean_digital` profile、无照片噪声、干净边缘必须写入测试；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/fixtures/regression_cases/case_d_clean_digital_number.json` 保存命令和 expected fields，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_case_d_clean_digital_keeps_photo_texture_and_noise_disabled` 验证 `clean_digital` profile 禁用 `photo_texture`/`photo_warp`、stage order 不含 `photo_texture`、dispatch 的 profile filter 移除 `photo_noise_delta` 且最终 patches 不含照片噪声、edge breakup、JPEG texture 或 photo warp 参数。
- [x] 每个回归 case 必须保存 expected report fields，而不是只比较最终图片是否存在；验证方式是 JSON assertion；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_regression_case_contracts.py::RegressionCaseContractsTest.test_all_regression_fixtures_store_commands_and_expected_report_fields` 验证每个 `tests/fixtures/regression_cases/case_*.json` 都保存 CLI command、profile 和至少 5 个 `expected_report_fields`，且 `test_case_a_*` 到 `test_case_d_*` 对这些 JSON 字段做实际断言，不以最终图片存在作为通过条件。

### R. 反模式门禁

- [x] 一个问题失败后不能把所有补丁族混合评分；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_dispatch_does_not_mix_all_patch_families_when_shape_blocks` 验证 `text_shape` 阻塞时混合的 ink/photo/background patch 会被拒绝，accepted patch 只能以当前 stage 主导。
- [x] 视觉模型说 `ok` 不能覆盖本地阶段失败；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_local_acceptance_gate.py::LocalAcceptanceGateTest.test_local_blocking_stage_overrides_visual_deliver`。
- [x] 字体没过时不能调 blur；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_text_shape_stage_rejects_photo_or_background_primary_patches`。
- [x] 粗细没过时不能先清灰边；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stroke_body_failure_blocks_gray_cleanup_and_photo_noise` 验证笔画体量失败时 `mask_threshold_delta`/background cleanup 补丁被拒绝。
- [x] 灰边过多时不能继续加 photo_noise 制造灰雾；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_ink_gray_stage_rejects_photo_noise_as_primary_fix` 验证 `ink_gray_balance` 阻塞时 `photo_noise_delta` 被拒绝。
- [x] 不能把某张图的左倾/右倾写成通用规则；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_no_hardcoded_special_cases.py::NoHardcodedSpecialCasesTest.test_runtime_code_and_prompts_do_not_encode_specific_names_images_or_target_char_rules` 验证 runtime code 和 packaged prompts 不含具体图片号、`本图`/`这个字` 或目标字符左右倾特例。
- [x] 不能为单个字写特殊规则；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_no_hardcoded_special_cases.py::NoHardcodedSpecialCasesTest.test_runtime_code_and_prompts_do_not_encode_specific_names_images_or_target_char_rules` 验证 runtime code 和 packaged prompts 不含具体人名或目标字调参规则。
- [x] 不能把通用 prompt 绑定到单个字段标签或默认参考字；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_no_hardcoded_special_cases.py::NoHardcodedSpecialCasesTest.test_runtime_code_and_prompts_do_not_encode_specific_names_images_or_target_char_rules` 验证 runtime code/prompt 不含字段专用标签、字段专用区域说法、默认参考字或字段前后专用说法。
- [x] 不能用更多迭代次数替代阶段判定；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_run_artifacts.py::RunArtifactsTest.test_revision_round_continuation_contract_requires_stage_direction` 验证 revision continuation contract 必须有当前 blocking stage 的 grid 或 stage patcher 方向，`test_revision_round_continuation_contract_rejects_max_rounds_only` 验证即使 `max_revision_rounds=12`，没有 stage-specific direction 也不能继续，`test_processing_service_records_revision_continuation_contract` 验证 `progress.jsonl`/`result.json` 轮次记录会写入 `revision_continuation_contract` 并在无方向时停在 `no_stage_specific_candidate_direction`。
- [x] 不允许“先交付，再靠用户肉眼指出问题”作为成功标准；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_local_acceptance_gate.py::LocalAcceptanceGateTest.test_local_blocking_stage_overrides_visual_deliver` 验证视觉模型给出 `pass/deliver` 时，只要本地 `stage_gate.blocking_stage` 不为空，本地会强制改为 `revise`，不能交付。

### S. 设计文档状态同步

- [x] `text_shape_joint_optimization_design.md` 的“现有流程差距”表必须同步当前状态：已实现项标记为已覆盖，未实现项链接到本 checklist 对应条目；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_design_alignment.py::DesignAlignmentTest.test_text_shape_gap_table_syncs_status_to_checklist_items` 验证差距表包含 11 个目标能力、只使用 `已覆盖`/`部分覆盖`/`未完成` 状态、每行链接 `local_flow_hardening_checklist.md`，并检查背景处理、同字数 CJK 等未完成项链接到对应 checklist 条目。
- [x] `staged_roi_pipeline_design.md` 的旧 7 类诊断关注点必须说明与当前 5 stage 结构的关系，避免两个阶段体系并存；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_design_alignment.py::DesignAlignmentTest.test_staged_design_declares_five_stage_gate_and_old_concern_mapping`。
- [x] README 中不能宣称本地流程完善完成，除非本节所有 `[ ]` 关闭；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_completion_claims.py::CompletionClaimsTest.test_readme_declares_hardening_status_while_checklist_is_open` 验证 checklist 仍有 `[ ]` 时 README 必须明确写出 local flow still being hardened，并指向本 checklist。
- [x] 提交或 PR 说明在声明关闭 checklist 项时必须引用具体 checklist 项，不能只写“优化流程”；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_checklist_commit_policy.py::ChecklistCommitPolicyTest.test_generic_process_message_fails` 验证泛泛 checklist 关闭说明失败，`test_message_with_closed_checklist_items_passes` 验证 `Closes checklist items ...` 格式能解析，`test_plain_feature_commit_is_not_a_checklist_closure_message` 验证普通功能提交不必伪造 checklist 关闭项，`scripts/validate_checklist_closure_message.py` 可用于 commit/PR message 文件校验。

### T. 代码落点和模块边界

- [x] request/result/progress/prompt stage context 的运行产物 helper 已从 `processing_service.py` 拆到 `src/roi_image_edit/run_artifacts.py`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_run_artifacts.py`。
- [x] `src/roi_image_edit/stages.py` 必须成为 stage contract 的唯一入口，承载 `StageSpec`、`StageResult`、detector mapping 和 prompt context；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_contracts.py::StageContractsTest.test_stage_contract_entrypoints_are_only_defined_in_stages_module` 和 schema contract 测试。
- [x] `src/roi_image_edit/stage_profiles.py` 必须成为 profile 定义和加载的唯一入口；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_profiles.py` 覆盖 profile 入口定义、profile matrix、显式 profile 覆盖自动建议 resolver 和 audit 字段。
- [x] `src/roi_image_edit/stage_patchers.py` 必须成为 stage patcher filter/dispatch 的唯一入口；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py` 覆盖 registry、allowed/blocked keys、dispatcher 依赖、旧混合入口缩减和具体 patcher runtime 直调禁用。
- [x] `local_validation.py` 中的 `local_*_issues` 必须继续收敛为 detector 底层函数，不能直接承担 stage policy；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_migration_contract.py::StageMigrationContractTest.test_stage_policy_does_not_call_local_issue_detectors` 验证 `stage_policy.py` 不导入 `local_validation`、不调用任何 `local_*_issues`，`test_local_validation_stage_gate_delegates_to_stages_module` 验证 `local_validation._stage_gate_for_report` 只委托 `stages.stage_gate_for_report`。
- [x] `revision_solver.py` 中的旧评分和候选选择逻辑必须继续收敛为 dispatcher/selector，不能重新生成跨阶段混合 patch；证据：`.venv/bin/python -m unittest discover -s tests`，`src/roi_image_edit/revision_selector.py` 承载 `final_acceptance_delivers`、`revision_selection_score`、`constrained_revision_params`，`tests/test_revision_selector_boundary.py::RevisionSelectorBoundaryTest.test_selector_functions_are_not_defined_in_revision_solver` 验证这些 selector 函数不再定义在 `revision_solver.py`，`test_region_processing_imports_selection_from_revision_selector` 验证 region revision loop 从 selector 模块导入且 `processing_service.py` 不再直接导入 selector，`test_final_font_revision_delegates_to_text_shape_grid_without_mixed_tuning` 验证 finalist 字体修订只委托 `text_shape_reset_candidates` 且不重新生成 opacity/blur/photo_noise/JPEG 混合候选，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_dispatch_does_not_mix_all_patch_families_when_shape_blocks` 继续验证 stage filter 拒绝跨阶段主补丁。
- [x] `processing_service.py` 不能继续承载 ROI 定位、候选生成、验收评分、修订求解器的长期核心逻辑；证据：`.venv/bin/python -m unittest discover -s tests`，`src/roi_image_edit/region_processing.py` 承载 `process_region`、`run_region_vision_checks`、`save_stage_candidate_evidence` 和 region-level candidate/revision 逻辑，`src/roi_image_edit/revision_selector.py` 承载 selection scoring 和 parameter constraints，`tests/test_processing_service_boundary.py::ProcessingServiceBoundaryTest.test_processing_service_keeps_only_payload_orchestration_defs` 验证 `processing_service.py` 只定义 payload 编排函数，`test_processing_service_imports_region_core_instead_of_defining_it` 验证它从 `region_processing.py` 导入 region core，`test_processing_service_does_not_call_image_candidate_or_revision_core` 验证它不直接调用 ROI plan、candidate generation、candidate scoring、stage patcher 或 revision solver 核心入口。
- [x] `iterative_pipeline.py` 的视觉 prompt 上下文必须只通过 stage context 结构传递阶段信息，不能手写散落的 prompt 拼接逻辑；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_iterative_pipeline_stage_context.py::IterativePipelineStageContextTest.test_stage_gate_is_attached_only_through_stage_context_helper` 验证 `iterative_pipeline.py` 只通过 `attach_report_stage_context` 写入 stage gate/context，`test_vision_prompt_payloads_use_stage_context_structures` 验证 candidate rank、tuning、final acceptance 的 prompt payload 都使用 `stage_context` 或 `stage_context_by_candidate` 结构。
- [x] `web_app.py` 必须只保留 HTTP/API/job 状态和本地 Web 服务启动，不承载阶段策略或图像处理逻辑；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_web_boundary.py` 验证 `web_app.py` 只能从 core 导入 `processing_service.process_payload`，且不能调用 stage、patcher、candidate generation 或图像处理入口。
- [x] `result.json` 和 `progress.jsonl` 必须是所有阶段证据的稳定外部接口；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_run_artifacts.py::RunArtifactsTest.test_external_artifact_schema_covers_result_progress_and_explanations` 验证 `external_artifact_schema_report()` 覆盖 stage、profile、candidate、patch、vision suggestion 和 rejection reason 字段，`test_result_audit_payload_strips_data_urls_but_keeps_stage_artifact_fields` 验证 `result.json` 审计保留 stage context、patch、model suggestions、rejection reason 和 stage evidence，`test_progress_record_adds_stable_schema_version` 验证 `progress.jsonl` 记录统一写入 `artifactSchemaVersion`。

## 已知实现证据

本节不是完成清单，只记录当前仓库里已经存在、后续可以复用的实现证据。它不能替代 A-T 的验收项。

- `reference_profile` 已有报告字段，包含旧文字、邻字、动态墨色阈值和动态核心变浅阈值。
- 固定 opacity 硬下限已移除或降级，改为动态墨色范围。
- 视觉反馈中的核心黑度、太黑、太淡、太硬、太细已经有结构化解析路径。
- 已有 stage severity、selected reason、模型建议截断记录和替代候选记录。
- 自动 ROI 已覆盖部分字段场景，但还不是所有字段通用路径。
- 背景指标已有白影、暗影、低纹理、纹理残差、ROI 扫描残差等结构化方向。
- Web/CLI 已有 accepted/rejected、blocking stage、迭代轮次、停止原因和候选抽屉展示。
- 既有烟雾产物包括 `output/web/20260605_020104`、`output/web/20260605_020350`、`output/web/20260605_024521`、`output/web/20260605_024811`；这些是历史证据，不等同于 Case A-D 可执行回归。

## 公开验收门槛

本地流程不能宣称完善完成，除非以下全部满足：

- [x] “设计目标转换情况”中的所有 `[ ]` 项全部关闭；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_checklist_integrity.py::ChecklistIntegrityTest.test_completion_gate_matches_open_item_count` 验证该总门槛被勾选时 checklist 中不存在未完成项，`tests/test_checklist_integrity.py::ChecklistIntegrityTest.test_every_checked_item_has_current_evidence_marker` 验证每个已关闭项都有当前证据。
- [x] A-T 中保留的每个 `[x]` 都有同条或相邻说明中的当前证据，证据不足时必须降级为 `[ ]`；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_checklist_integrity.py::ChecklistIntegrityTest.test_every_checked_item_has_current_evidence_marker` 验证每个 `[x]` 行必须包含 `证据：`，`test_checked_item_evidence_paths_exist` 验证证据中的 `tests/`、`scripts/`、`docs/`、`src/` 路径真实存在，`test_checked_items_use_current_verification_command_or_stable_artifact` 验证证据必须引用当前测试命令或稳定产物。
- [x] 回归 Case A-D 已转成可执行用例并通过；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_regression_case_contracts.py` 读取 `tests/fixtures/regression_cases/case_a_field_text_shape.json`、`case_b_shorter_cleanup.json`、`case_c_longer_protected.json`、`case_d_clean_digital_number.json` 并执行 stage gate、local acceptance gate、slot quality、placement strategy、profile-aware dispatch 和 JSON expected field assertion。
- [x] 每个 stage patcher 的声明参数和拒绝跨阶段参数测试通过；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_stage_patcher_registry.py::StagePatcherRegistryTest.test_stage_patchers_declare_primary_allowed_and_blocked_keys` 验证每个 stage patcher 声明 primary、allowed、blocked 且 allowed/blocked 不冲突，`test_stage_patcher_outputs_do_not_contain_undeclared_keys` 验证输出不含未声明参数，`test_filter_report_declares_secondary_impacts_and_rejects_cross_stage_primary` 验证跨阶段 patch 会被拒绝或声明次级影响。
- [x] README、设计文档和 prompt 不再声称未验证能力已经完成；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_completion_claims.py::CompletionClaimsTest.test_public_docs_and_prompts_do_not_claim_unverified_completion` 扫描 README、`docs/*.md` 和 packaged prompts，禁止无条件当前完成态措辞如 `local flow is complete`、`本地流程完善完成`、`所有设计目标已完成`，但允许 checklist 以否定或条件形式列出门槛。
- [x] 所有交付任务都能从 `result.json`、`progress.jsonl`、stage evidence 和候选图解释通过或失败；证据：`.venv/bin/python -m unittest discover -s tests`，`tests/test_run_artifacts.py::RunArtifactsTest.test_delivery_artifact_manifest_explains_rejected_region_with_stage_candidates` 验证 `artifact_manifest.json` 统一索引 result/progress、stage top candidate、视觉候选图、最终 compare 和 slot quality report，`tests/test_failure_artifacts.py::FailureArtifactsTest.test_auto_roi_failure_preserves_rejected_artifacts_and_result_evidence` 验证真实 `process_payload` 失败路径写出 `artifact_manifest.json`、`run_finished.all_explainable=true`，且失败图像可由 failure report、rejected input 和 pre-candidate gate report 解释。

任何一项未满足，都不能宣称本地流程完善完成。
