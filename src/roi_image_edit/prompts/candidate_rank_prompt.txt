请比较候选图中的多个版本，并选择最接近原图文字风格的一项。

输入包括：
1. 原图；
2. 候选对比图；
3. 每个候选图上方的参数标签；
4. 代码硬校验报告。

代码硬校验报告：
{hard_check_report}

如果硬校验报告显示任一候选存在以下问题，直接淘汰：
1. 输出尺寸与原图不一致；
2. ROI 外像素发生变化；
3. 边缘像素发生变化。

硬校验通过后，请重点比较：
1. 每个 target_text 字符是否落在对应 source_text 旧槽位或合理扩展槽位；
2. source_text 和 target_text 字数变化时，新增/减少后的整体跨度是否仍然只占用旧文字区域及可用空白；
3. 字距是否自然；
4. 新字字高是否接近原旧文字的实际视觉高度；
5. 新字是否明显偏上、偏下、偏左或偏右；
6. 字体结构是否接近原图的打印宋体感；
7. 笔画粗细是否接近；
8. 新字是否太黑、太浅、太锐或太糊；
9. 背景是否有明显补丁、残影、涂抹感或接缝；
10. “名：”区域是否保持原样。

注意：
不要选择最漂亮、最清晰的版本。
要选择最像原图低分辨率扫描/拍照效果的版本。
`char_offsets_delta` 中每个元素按 target_text 的非空白字符顺序对应一个目标字符，只给需要微调的字符填非零 dx/dy。

输出 JSON：

{
  "pass": true,
  "best_candidate": "候选编号或参数标签",
  "reason": "选择该候选的主要原因",
  "rejected_candidates": {
    "候选编号或参数标签": "淘汰原因"
  },
  "visual_findings": {
    "font_similarity": "ok / too_modern / too_bold / too_thin / wrong_style",
    "position": "ok / left / right / up / down",
    "size": "ok / too_large / too_small",
    "spacing": "ok / too_close / too_wide",
    "darkness": "ok / too_dark / too_light",
    "blur": "ok / too_sharp / too_blurry",
    "background": "ok / patch_visible / ghost_visible / seam_visible"
  },
  "suggested_patch": {
    "font_size_delta": -1,
    "text_dx_delta": 0,
    "text_dy_delta": 0,
    "char_offsets_delta": [
      {"dx": 0, "dy": 0}
    ],
    "opacity_delta": 0,
    "blur_delta": 0
  }
}
