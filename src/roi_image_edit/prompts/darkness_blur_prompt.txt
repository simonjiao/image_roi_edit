请只比较新文字的黑度、灰度边缘和模糊程度。

输入包括：
1. 原图；
2. 多个透明度/模糊半径候选图；
3. 每个候选图的 opacity 和 blur 参数；
4. 代码硬校验报告。

代码硬校验报告：
{hard_check_report}

代码硬校验报告中如果包含 `stage_context`，必须服从本地阶段顺序。
本 prompt 只对应 `ink_gray_balance` 和 `photo_texture` 阶段内的黑灰、灰边和模糊判断，不是独立 stage。
如果 `stage_context.blocking_stage` 是 `text_shape` 或 `background_cleanup`，可以描述黑灰/模糊观察，但不要把 opacity、blur、noise 或压缩作为当前轮主修复方向。
公开阶段只能是：
`hard_boundary`、`text_shape`、`ink_gray_balance`、`photo_texture`、`background_cleanup`。

判断重点：
1. 新字是否比原字明显更黑；
2. 新字是否比原字明显更浅；
3. 新字边缘是否过锐，像后贴上去的；
4. 新字是否过糊，导致可读性下降；
5. 新字边缘灰度是否接近原图扫描/拍照效果；
6. 新字是否与 source_text 旧槽位、同一行参考文字、字段标签或 protected_texts 的黑度协调；
7. 背景中是否存在旧字残影。

不要选择纯黑、最清楚的版本。
请选择最接近原图清晰程度和灰边效果的版本。

输出 JSON：

{
  "blocking_stage": "ink_gray_balance / photo_texture",
  "direction": "reduce_true_black_core / recover_core_black / trim_outer_gray_halo / add_photo_texture",
  "best_opacity": "候选 opacity 参数",
  "best_blur": "候选 blur 参数",
  "reason": "为什么选择该透明度和模糊半径",
  "visual_findings": {
    "darkness": "ok / too_dark / too_light",
    "edge_gray": "ok / too_hard / too_soft",
    "sharpness": "ok / too_sharp / too_blurry",
    "integration": "ok / looks_pasted"
  },
  "suggested_next": {
    "opacity": "候选 opacity 参数",
    "blur": "候选 blur 参数"
  }
}
