请对最终局部文字修图结果进行验收。

输入包括：
1. 原图；
2. 最终修图结果；
3. 原图与最终结果的放大对比图；
4. 最终使用的参数；
5. 代码硬校验报告。

最终使用参数：
{final_params}

代码硬校验报告：
{hard_check_report}

硬性验收规则：
1. 如果输出尺寸与原图尺寸不一致，必须 pass=false。
2. 如果 ROI 外变化像素数不为 0，必须 pass=false。
3. 如果边缘变化像素数不为 0，必须 pass=false。
4. 如果“名：”区域被修改，必须 pass=false。
5. 如果图片被拉伸、缩放、裁剪、重采样，必须 pass=false。
6. 只有硬校验全部通过，才进入视觉验收。

视觉验收重点：
1. target_text 是否自然替代 source_text；
2. 每个目标字符是否贴近对应旧槽位或合理扩展槽位；
3. 新文字的位置、字距、基线是否接近原图；
4. 字体风格是否与原图一致或至少不突兀；
5. 字号是否明显偏大或偏小；
6. 笔画粗细是否明显不一致；
7. 黑度是否明显过黑或过浅；
8. 清晰度是否明显过锐或过糊；
9. 背景是否有明显补丁、残影、涂抹感或接缝；
10. ROI 边界是否有突兀过渡；
11. 整体是否可以接受为同一张图中的文字。

验收口径：
- pass：硬校验通过，视觉上无明显突兀，可交付。
- marginal：硬校验通过，但视觉上有轻微问题；可交付但建议小幅优化。
- fail：硬校验失败，或视觉上明显不自然。

输出 JSON：

{
  "pass": true,
  "acceptance_level": "pass",
  "reason": "硬校验通过，视觉上可接受为同一张图中的文字。",
  "hard_check": {
    "size_match": true,
    "outside_roi_unchanged": true,
    "border_unchanged": true,
    "protected_text_unchanged": true
  },
  "visual_findings": {
    "char_positions": "ok / left / right / up / down / mixed",
    "spacing": "ok / too_close / too_wide",
    "baseline": "ok / shifted",
    "font_similarity": "ok / slightly_off / wrong_style",
    "size": "ok / too_large / too_small",
    "stroke_weight": "ok / too_bold / too_thin",
    "darkness": "ok / too_dark / too_light",
    "sharpness": "ok / too_sharp / too_blurry",
    "background": "ok / patch_visible / ghost_visible / seam_visible"
  },
  "must_fix": [],
  "optional_tuning": [
    {
      "issue": "如果某个目标字符仍略偏左",
      "suggestion": "char_offsets_delta 中对应字符 dx=+1"
    }
  ],
  "final_decision": "deliver / revise"
}
